using UnityEngine;

public class PlayerSelect : MonoBehaviour
{
    public int miniPekka;
    public int skeleton;
    public int valkyrie;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
